name: extraction_de_texte_sur_image
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_ml_kit: ^0.16.3
  cupertino_icons: ^1.0.2
  camera: ^0.10.5+9
  video_player: ^2.8.2
  image_picker: ^1.0.7
  mime: ^1.0.4
  path_provider: ^2.1.2
  google_mlkit_text_recognition: ^0.11.0
  animated_text_kit: ^4.2.2
  
 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
